<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/home_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".HomeFragment">

    <!-- updated empty state (replace the previous no_post_container block) -->
    <LinearLayout
        android:id="@+id/no_post_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        android:padding="24dp">

        <ImageView
            android:id="@+id/no_post_image"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:src="@drawable/no_data_illustrator"
            android:contentDescription="No post"
             />

        <TextView
            android:id="@+id/no_post_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:text="No announcements yet"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/no_post_sub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="There are currently no posts. Only school administrators can publish announcements here."
            android:textColor="@color/black_low"
            android:textSize="14sp"
            android:lineSpacingExtra="2dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:gravity="center"/>

    </LinearLayout>

    <!-- Swipe to refresh + RecyclerView (main content) -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/homeRecycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="true"
            android:paddingBottom="12dp"
            android:scrollbars="none"
            />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Shimmer skeleton overlay while loading -->
    <com.facebook.shimmer.ShimmerFrameLayout
        android:id="@+id/shimmerLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:visibility="gone"
        app:shimmer_auto_start="true"
        app:shimmer_duration="900">

        <!-- Stack a small set of skeleton items to mimic feed -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="16dp"
            android:paddingBottom="16dp">

            <!-- include multiple skeleton rows to fill area -->
            <include layout="@layout/post_skeleton" />
            <View android:layout_height="12dp" android:layout_width="match_parent" />
            <include layout="@layout/post_skeleton" />
            <View android:layout_height="12dp" android:layout_width="match_parent" />
            <include layout="@layout/post_skeleton" />
            <View android:layout_height="12dp" android:layout_width="match_parent" />
            <include layout="@layout/post_skeleton" />

        </LinearLayout>
    </com.facebook.shimmer.ShimmerFrameLayout>

</FrameLayout>